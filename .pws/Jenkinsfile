pipeline {
    agent any
    options {
        skipDefaultCheckout()
    }
    stages {
        stage('Prepare') {
            steps {
                // Tests ?
                echo "Preparing"
            }
        }
        stage('Deploy') {
            when {
                beforeAgent true
                tag "pws/deploy"
            }
            agent { label 'eu-petro-ws' }
            steps {
                dir ("/home/www/petro.ws/blog") {
                    scm checkout
                }
            }
            post {
                cleanup {
                    deleteDir()
                }
            }
        }
    }
    post {
        cleanup {
            deleteDir()
        }
    }
}
